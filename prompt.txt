INITIAL PROMPT:

CONTEXT :Language/framework + version: JavaScript (ES6+), no framework Where it runs: local What changed recently: none — initial implementation

THE BUG : 
Expected behavior: splitBill applies the tip exactly once, splits the resulting total evenly across N people in whole cents, and all shares sum back to the exact grand total — with any leftover cent explicitly assigned to specific people. 
Actual behavior: Tip gets double-counted and/or the extra cent is mis-assigned, so per-person shares don't sum to the true total.

Reproduction steps:

Call splitBill(10.03, 15, 3)
Observe the returned per-person shares
Sum them → does not equal the correct bill+tip total
Frequency: always — deterministic, not intermittent

CONSTRAINTS : Do NOT touch: function names/signatures (splitBill, calculateTotal) — keep the public API stable Do NOT introduce: new dependencies Must remain compatible with: callers passing (bill, tipPercent, numPeople) and expecting an array/object of per-person shares


SECOND PROMPT:
OUTPUT FORMAT
Root cause explanation
Fix as a diff/patch
One-line commit message
No speculative fixes — only propose a fix once root cause is confirmed


THIRD PROMPT:
Write a full unit test suite for the fixed splitBill function to prove correctness and prevent regressions.

REQUIREMENTS
Cover at minimum:
The original bug scenario: splitBill(10.03, 15, 3) — shares must sum to exactly the bill+tip total
0% tip
Evenly divisible total (no remainder to assign)
Remainder of 1 cent, remainder of 2 cents (party size 3, 4, 5+)
Single person (numPeople = 1) — gets 100% of total
Large party size (e.g. 10+ people) with a small bill
Every test asserts sum(shares) === totalCents/100 exactly, not just "close enough"

OUTPUT FORMAT
One test file (tip-splitter.test.js)

FOURTH PROMPT :

Now write a simple UI for this tip splitter that uses the fixed tip-splitter.js (but don't inline the function — keep it in its own module or file and import it).

REQUIREMENTS

UI layout (keep it simple and clean):

Input for bill amount (in dollars)
Input for tip percent (default to 18)
Input for number of people (default to 2)
Quick-tip buttons for 10%, 15%, 18%, 20%, 25% that update the tip % input
A summary row showing: Subtotal, Tip, Grand Total
A results section showing per-person breakdown (table with Person, Share, Note)
Include a "Sum Check" at the bottom that explicitly sums the shares and shows if they match the Grand Total (with a visual indicator like ✅ / ⚠️)
Style with simple CSS (no framework). Make it look nice but don't go overboard — focus on clarity.
Keep it all in one file for now: index.html + embedded CSS + embedded script that uses the tip-splitter.js module.

FIFTH PROMPT:
Behave like quality engineer/test engineer who is responsible for the ui tasks
Create a one python file having the different test case help to detect that the bug is resolved or not.
So delivearble is : Pytest file having the script for testing with resolved installatio dependencies
Note: Use playrights for python for the ui testing task.